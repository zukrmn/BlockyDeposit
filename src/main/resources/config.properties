# file | http
mode=file
output_dir=out
endpoint_url=
connect_timeout_ms=1500
read_timeout_ms=2000

# filtering
# inclusive = ONLY items listed are allowed
# exclusive = All items allowed EXCEPT the listed ones
filter_mode=exclusive
# comma-separated list; supports enum names (STONE, DIRT, IRON_AXE) or numeric legacy IDs (e.g., 3, 4)
filter_items=
